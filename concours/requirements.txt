# Dépendances LogX AI — versions épinglées pour des builds/CI reproductibles.
# Runtime : le cœur du serveur n'utilise que la bibliothèque standard Python.
# Les libs ci-dessous couvrent la validation des règlements et le matériel.

# Validation des définitions de concours (logx_validate.py)
jsonschema>=4.20,<5

# Pilotage CAT série natif (logx_cat.py) — optionnel : import protégé par
# HAS_PYSERIAL, l'appli démarre sans si aucune radio série n'est pilotée.
pyserial>=3.5,<4

# Keyer vocal TTS hors-ligne (logx_voicekeyer.py) — optionnel, Windows/SAPI5.
pyttsx3>=2.90

# Keyer vocal, voix Piper (logx_voicekeyer.py) — moteur neuronal local,
# nettement plus naturel que SAPI5, SANS abonnement ni réseau à l'usage.
# PAS une dépendance de ce projet : invoqué en sous-processus (comme
# sounddevice s'appuie sur PortAudio), donc rien à ajouter ici. L'opérateur
# installe `pip install piper-tts` (fournit la commande `piper`) et
# télécharge un modèle de voix .onnx sur https://huggingface.co/rhasspy/piper-voices
# une fois, puis renseigne le chemin du modèle en CONFIG (KEYER VOCAL).

# Lecture audio du keyer vocal (logx_voicekeyer.py) — sans lui, la synthèse
# fonctionne mais la liste des périphériques et la diffusion échouent.
sounddevice>=0.4

# EME/rebond lunaire (logx_eme.py) — optionnel, import protégé par HAS_EPHEM.
# Éphéméride VSOP87/ELP2000 embarquée, aucune donnée réseau. Wheel précompilé
# sous Windows/Linux/macOS (pas de compilateur requis).
ephem>=4.1

# Tests (suite pytest sous concours/tests/)
pytest>=8,<10

# Lecture des formulaires d'issue GitHub (.github/ISSUE_TEMPLATE/*.yml) par
# test_release_ci_config.py — jamais importé par le code applicatif.
PyYAML>=6,<7

# Exécution réelle de logx_logbook.js (moteur V8) par
# test_logbook_render_window_reset.py, pour reproduire un scénario client
# (fenêtre de rendu du logbook) sans dépendre d'un navigateur/Selenium.
# Jamais importé par le code applicatif ; test ignoré (importorskip) si absent.
py_mini_racer>=0.6,<1

# ── Optionnels non installés par défaut ──────────────────────────────────────
# Extraction PDF des règlements (logx_rules.py / logx_rules_ai.py) : cascade
# fitz (pymupdf) -> pdfplumber -> pypdf, avec repli final sans dépendance.
# Décommenter pour une extraction de meilleure qualité :
# pymupdf>=1.24

# Publication MQTT optionnelle des événements shack (logx_mqtt.py) : QSO/
# fréquence radio/score vers un broker local (Mosquitto...), pour un tableau
# de bord externe (Node-RED, Home Assistant...). Désactivée par défaut, import
# protégé par HAS_PAHO — le reste de l'appli fonctionne à l'identique sans.
# Épinglé <2 : paho-mqtt 2.0 a changé la signature de Client() (paramètre
# callback_api_version requis), non nécessaire pour l'usage simple fait ici.
# Décommenter pour l'activer :
# paho-mqtt>=1.6,<2
