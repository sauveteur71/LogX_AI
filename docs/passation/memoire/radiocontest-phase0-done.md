---
name: radiocontest-phase0-done
description: "Phase 0 du plan de bataille RadioContest AI terminée le 15/07/2026 — serveur.py découpé en 8 modules, CONTEST_SCHEMA créé, prochaine étape Phase 1 (UX)"
metadata: 
  node_type: memory
  type: project
  originSessionId: e727f52a-b66b-4617-abb1-afba96fda04d
---

Le projet RadioContest AI (logiciel de contest radioamateur F6KQJ, dossier `concours/`) suit le plan en 5 phases de `Plan_Bataille_RadioContest_AI.md`.

**Phase 0 terminée le 15 juillet 2026 :**
- `serveur.py` (monolithe 4029 lignes) découpé en modules : `utils.py`, `contest_definitions.py`, `storage.py`, `rules.py`, `scoring.py`, `clusters.py`, `prompts.py`, `http_handler.py` + `serveur.py` réduit au point d'entrée. Original conservé dans `serveur.py.bak_pre_phase0`.
- Points délicats du découpage : `rules_db` et `EXTERNAL_CONTESTS_CACHE` sont RÉASSIGNÉS à chaud (mise à jour annuelle) → toujours y accéder via `rules.rules_db` / `rules.EXTERNAL_CONTESTS_CACHE`, jamais par `from rules import rules_db`. `shared_log` (storage.py) est muté en place uniquement (`shared_log[:] = ...` dans load_log_from_disk) pour que les imports par nom restent valides.
- Bug corrigé : la route POST `/data/spots` (push spots HF depuis le navigateur) était morte dans `do_GET` ; déplacée dans `do_POST`.
- `contest_schema.json` (JSON Schema 2020-12) + `validate_definitions.py` : contrat formel d'un concours, garde-fou des Phases 2/3/4. Les 22 concours (pas 25) sont conformes.
- L'item D du rapport de juin (backup localStorage 5 min) était déjà implémenté dans logbook.html (`backupLog()`).

**Phase 1 (UX) terminée le 15 juillet 2026 :**
- `statusbar.js` (nouveau, inclus sur les 4 pages HTML) : concours actif, compte à rebours, dernier backup, dernier check règlements.
- Assistant "nouveau concours" : configuration.html lit désormais `?contest=ID` (handleContestParam) → sélection + filtres + dates auto + bannière de lancement rapide.
- Sélecteur unifié : la grille config fusionne liste locale + `/data/calendar` (badge "✓ règlement suivi") + `/data/external_contests` (badge "à confirmer", ids `EXT_<ref>`).
- Mode débutant/expert : bouton 🎚, classe CSS `.expert-only`, localStorage `rc_ui_mode`, l'étape 4 est sautée en mode simple.
- `/data/calendar` expose `duration_h` + `start_utc` pour le calcul de dates côté client (`serverContestDates()`).
- `.claude/launch.json` : serveur lancé avec `cwd: concours` (les caches JSON sont relatifs au cwd).
- Nota : les captures d'écran du Browser pane timeout sur ces pages (animations CSS ?) — vérifier via read_page/javascript_tool à la place.

**Phase 2 (moteur de score générique) terminée le 15 juillet 2026 :**
- `calc_qso_value` (scoring.py) = moteur à briques : PREDICATES (prédicats nommés), MULT_EVALUATORS (7 familles de multiplicateurs), LEGACY_SCORING_PRESETS (conversion des 10 types historiques). Déclaration explicite : `'scoring': {'bricks': {...}}` dans une définition.
- Équivalence prouvée par test d'or : 44 064 comparaisons contre l'ancien moteur (serveur.py.bak_pre_phase0), zéro différence sur les types vivants. Le script est dans le scratchpad de session (test_equivalence.py) — le recréer au besoin depuis le backup.
- Bug historique corrigé : prefix_multiplier/power_state/summit_points/park_points n'avaient AUCUNE branche (WPX, ARRL DX, SOTA, POTA scoraient 0). Ils scorent maintenant.
- validate_definitions.py valide les briques contre le moteur (imports depuis scoring.py). contest_schema.json décrit le format bricks.

**Phase 3 (lecture IA des règlements) terminée le 15 juillet 2026 :**
- `rules_ai.py` : analyze_rules(url/texte, cfg) → téléchargement + extraction (pymupdf en 1er choix, fallbacks pdfplumber/pypdf/regex dans rules.py:extract_document_text) → IA (provider de current_config) → validation → proposition avec citations. Ne sauvegarde jamais.
- Endpoints : POST /rules/analyze, /rules/save_definition (re-valide côté serveur), /rules/delete_custom. UI : configuration.html étape 2, section « 🤖 ANALYSER UN RÈGLEMENT » + modal de relecture (openReviewModal/saveReviewedDefinition).
- Persistance : custom_contests.json fusionné dans CONTEST_DEFINITIONS au chargement (contest_definitions.py:load_custom_contests, CUSTOM_CONTEST_IDS pour le badge « 🤖 IA + RELECTURE »).
- Grammaire de dates : rules.py:DATE_RULE_PATTERN = source de vérité unique (calc, validation, prompt IA). nth_full_weekend ajouté.
- Test réel réussi le 15/07 : WAEDC extrait de la page DARC (HTML anglais), enregistré dans custom_contests.json. La restriction EU↔DX avait été ratée → briques `validity` ajoutée + leçon injectée dans le prompt.

**Phase 3+ (extraction de classe mondiale) terminée le 16 juillet 2026 :**
- rules_ai.py v2 : PDF natif Anthropic (base64 document block, garde-fous 25 Mo/90 pages), sortie JSON forcée (tool use avec contest_schema comme input_schema ; json_object OpenAI ; responseMimeType Gemini), passe de vérification adversariale (VERIFICATION_CHECKLIST de 8 pièges, corrections validées avant application), multilingue.
- Nouvelles briques scoring.py : filtre 'bands' sur les règles de points, 'mult_weight_by_band'. Test d'or 44 064 toujours identique (les concours en briques sont exclus de la comparaison ancien moteur).
- Partage : GET /rules/export_custom, POST /rules/import_custom (re-validation à l'import), boutons UI dans configuration.html étape 2.
- eval_extraction.py (dans concours/) : corpus de 3 règlements réels, à lancer avec ANTHROPIC_API_KEY ; --mock pour le harnais, --no-verify pour mesurer l'apport de la 2e passe.
- PIÈGE d'exploitation : le .bat réutilise un serveur déjà lancé — après une mise à jour du code, fermer la fenêtre « RadioContest Serveur » sinon l'ancien code continue de servir (vécu : instance de la veille → 404 sur les nouvelles routes).

**Phase 4 (couverture calendrier) terminée le 16 juillet 2026 :**
- Pipeline WA7BNM réparé : le parseur de fetch_wa7bnm_calendar (rules.py) attendait du markdown, le site sert du HTML → réécrit (month via <strong>, rows via href contestdetails ref=). 358 concours/an en cache, refresh auto si cache vide/autre année/>7 jours (load_external_contests).
- 13 contests EU/monde ajoutés à CONTEST_DEFINITIONS (section "EUROPE & MONDE") : WAEDC_SSB/RTTY, UBA_DX_SSB/CW, EU_HF_CHAMP, RUSSIAN_DX, SP_DX, HA_DX, ALL_ASIAN_CW/SSB, STEW_PERRY, ARRL_10M, ARRL_160M. Tous en briques ; barèmes incertains marqués "à confirmer via 🤖" (HA_DX notamment). EU Sprint et YOTA volontairement exclus (multi-éditions).
- Moteur : prédicats is_asia/is_eu, prefix_in sur règles de points ET sur validity (dict), heure optionnelle sur les règles full_weekend (_13h). 36 concours en base au total.

**eval_extraction.py lancé en réel le 16/07/2026 : 19/19 champs corrects après corrections.**
- Blocage initial : Avast Web Shield intercepte tout le HTTPS de la machine et Python 3.13 (VERIFY_X509_STRICT par défaut) rejette son certificat racine → TOUTES les requêtes HTTPS échouaient. Corrigé par `utils.py:SSL_CTX` (vérification complète conservée, flag strict retiré = comportement 3.12) passé en context= dans utils.fetch_url, rules.download_rules_document, rules_ai._http_json/_download_bytes, http_handler (3 proxys IA). La vieille note « sites radioamateur aux certificats cassés » dans utils.py était en réalité déjà Avast.
- Erreur d'extraction trouvée puis corrigée : CQWW datait `last_saturday_october` au lieu de `last_full_weekend_october` (divergent en 2026 : le 31/10 est un samedi → 31/10 vs 24-25/10). Leçon « weekend ⇒ *_full_weekend_*, jamais *_saturday_* » injectée dans le prompt d'extraction ET la checklist de vérification (rules_ai.py). Re-test CQWW : 6/6.
- Attente RPH assouplie dans le corpus : `first_saturday_july(_14h)?` (l'IA extrait l'heure de départ, plus précis).
- La clé API est dans `concours/clef API.txt` (gitignoré) ; l'éval se lance avec `PYTHONUTF8=1` (sinon UnicodeEncodeError cp1252 sur les ═) et `ANTHROPIC_API_KEY=$(tr -d '\r\n' < 'clef API.txt')`.

**Préparation Phase 5 faite le 16/07/2026 (commits 8d45fa1 + eb0f0d1) :**
- Définition WAEDC_CW (custom_contests.json) relue : cohérente avec les WAEDC_SSB/RTTY intégrées, dates 2026 exactes (08-09/08), moteur vérifié (EU↔EU rejeté, poids ×4/×3/×2 par bande OK). QTC non modélisés (en note, connu).
- Définition EU_HF_CHAMP corrigée avec le PDF officiel 2026 (règles du 30/06/2026, analysées par le pipeline IA du projet — les URLs lea.hamradio.si sont mortes, le site officiel est euhf.s5cc.eu) : validity 'is_eu' ajoutée (seuls les QSO Europe comptent — bug réel, W1AW passait), mult zone_dxcc retiré (vrai mult = années de 1re licence distinctes/bande, inconnaissable au spot), deadline 48 h, self-spot autorisé en 2026.
- Répétition UI EU HF Champ OK : configuration.html?contest=EU_HF_CHAMP sélectionne la carte, dates auto (01/08 12:00→02/08 00:00), statusbar présente (id DOM : rcStatusBar, PAS rc-statusbar).
- Idée notée pour plus tard : évaluateur de mult 'exchange_distinct' (valeurs d'échange distinctes par bande) pour scorer EUHFC/similaires depuis le log réel.

**Coach stratégique + refonte UX faits le 16/07/2026 (commit a52212e) :**
- `coach.py` (nouveau) + GET /coach/state : horloge concours (lit la config CLIENT plate : contest, contest_start_date, contest_end_date, contest_end_utc — l'heure de départ vient de la définition start_utc), rythme QSO/h + silence radio, plan de bande par heure UTC pondéré par mult_weight_by_band, budget off-time, rappels QTC/fin de course/envoi log. 100 % déterministe ; le LLM ne sert que via le bouton 🧠 COACH (coach_prompt → /proxy/ai). Panneau coach dans radiocontest.html (poll 60 s).
- Nouveaux champs schéma+définitions : op_time_max_h, off_time_min_min, qtc (WAEDC ×3).
- UX : nav unifiée 4 pages (CONFIG→LOGBOOK→CARTE IA→CALENDRIER), launchApp() → logbook.html par défaut, 7 champs EDI en expert-only (étape 1 config), mode simple GLOBAL appliqué par statusbar.js (localStorage rc_ui_mode), logbook : 23 alert() → notify() (toast .macro-toast non bloquant), macros+ON4KST en expert-only.
- Constat d'audit : le classement scoring (rank_stations_by_value) n'atteint le front que via le texte LLM re-parsé (parseStations) — amélioration possible : exposer le ranked JSON dans /coach/state ou /data/refresh.

**Renommage global le 16/07/2026 (commit def7c8a) — TOUS les fichiers programme ont le préfixe `radiocontest_` :**
- serveur→radiocontest_serveur.py, utils→radiocontest_utils.py, contest_definitions→radiocontest_definitions.py, storage/rules/rules_ai/scoring/clusters/prompts/coach→radiocontest_*.py, http_handler→radiocontest_http.py, validate_definitions→radiocontest_validate.py, eval_extraction→radiocontest_eval.py.
- Pages : configuration→radiocontest_configuration.html, logbook→radiocontest_logbook.html, calendrier→radiocontest_calendrier.html, radiocontest.html→radiocontest_carte.html, rallye-vhf-terrain→radiocontest_terrain.html, statusbar.js→radiocontest_statusbar.js.
- Style d'import : `import radiocontest_rules as rules` (les accès module.attr restent valides, dont rules.rules_db). Anciennes URL servies via LEGACY_PAGES (radiocontest_http.py). Fichiers de données INCHANGÉS (config.json, shared_log.json, caches, contest_schema.json, clef API.txt).
- Lancement : `python radiocontest_serveur.py` depuis concours/ ou LANCER_RADIOCONTEST.bat. Toute mention des anciens noms dans cette mémoire (avant ce paragraphe) se lit avec le préfixe radiocontest_.
- Correctifs calendrier + thème (commit 89af8f3) : le client regroupait par numéro de mois (janvier d'abord) alors que /data/calendar arrive DÉJÀ triée depuis aujourd'hui → regroupement séquentiel (année, mois) ; filtre « À VENIR (31 j) » = en cours ou ≤31 jours ; thème rc_theme appliqué globalement par radiocontest_statusbar.js (palette day-mode requise par page) ; radiocontest_terrain.html → radiocontest_mobile.html (le logiciel est multi-concours, plus de marque « Rallye »).
- Page mobile GÉNÉRALISÉE (commit 017eaa7, usage « mobile ou à domicile » voulu par l'utilisateur) : URLs relatives (les localhost:8080 en dur cassaient depuis un téléphone), prompt IA chargé depuis /data/system_prompt (concours actif), identité/position depuis /config.json, tuile SCORE TOTAL générique, plus de modal de clé API (tout passe par /proxy/ai, clé côté serveur).

**Durcissement complet le 16/07/2026 (14 tâches, commits 939acc8→2450c3c) :**
- Sécurité : traversée de répertoire corrigée (_resolve realpath + _NEVER_SERVE pour « clef API.txt » qui devenait servable une fois l'URL décodée) ; config/calldb/shared_log sortis de git (+config.example.json ; AUCUNE clé réelle dans l'historique, que des placeholders) ; auth token (cookie SameSite=Strict via pages HTML, .auth_token gitignoré, exigé sur POST + /data/update_rules) + CORS limité aux IP privées ; /debug/* désactivés par défaut (server.debug).
- Robustesse : save_json_atomic (temp+os.replace, calldb_lock dédié) ; do_refresh parallélisé (ThreadPoolExecutor, deadline 25 s, une fonction par source) ; _cfg_snapshot() partout ; _raw() factorise les réponses ; prints serveur cp1252-sûrs (l'éval tourne SANS PYTHONUTF8 désormais).
- Qualité : tests/ + pytest.ini, 38 tests (utils, scoring, sécurité, exports, storage) — a débusqué un BUG moteur : _mult_zone_dxcc comparait l'indicatif au set des zones → chaque station passait pour « NOUVELLE ZONE » ; corrigé (nouveauté DXCC seule détectable, pas de table indicatif→zone). Logbook JS externalisé dans radiocontest_logbook.js (126 fonctions vérifiées, HTML 4068→915 lignes).
- Capacités : /log/add détecte les doublons (409 + force:true, confirm côté logbook, resync hors-ligne forcée) ; radiocontest_export.py + GET /log/export/cabrillo|adif ; log migré vers radiocontest.db SQLite (table qso indexée, migration one-shot des 71 QSO vérifiée, /log/reset ARCHIVE dans qso_archive au lieu de perdre, shared_log.json reste un backup lisible, API HTTP inchangée).

**Page PROPAGATION créée le 16/07/2026 (commit 9b27160), radiocontest_propagation.html, nav 📶 PROPAG sur les 5 pages :**
- Sources : hamqsl.com/solarxml.php (N0NBH : SFI/A/K/taches/xray/aurore + conditions par bande jour-nuit + E-Skip Europe, cache 15 min, clusters.fetch_solar_data) et prop.kc2g.com/api/stations.json (MUF réelle = foF2×M(3000)F2 de l'ionosonde fraîche <3 h la plus proche du locator, ex. Dourbes pour la France, clusters.fetch_muf).
- Endpoints : GET /data/propagation (solaire+MUF), GET /data/spots_ranked (need list STRUCTURÉE : le classement du moteur atteint enfin le front — refactor scoring.build_ranked_spots partagé avec build_scoring_context, spots depuis SPOTS_CACHE+browser sans re-fetch).
- scoring._band_from_freq : bande réelle dérivée de la fréquence pour les spots HF (3512→'3.5') — nécessaire aux mults pondérés WAE.
- Rappel : dupe check à la frappe, taux horaire (coach + graphe logbook), locator→distance/azimut (boussole), échange affiché existaient déjà.

**Passe E1+A2+A3+C1 le 16/07/2026 (commits 7dfedc6 + 6199721), issue de TODO_CAPACITES.md (Downloads) :**
- E1 : radiocontest_dxcc.py = parseur cty.dat AD1C (4482 préfixes + 2670 =CALL, zones dérogatoires (cq)[itu], /P et F/ON4ABC). Branché au moteur : dx_country=clé cty ('K','DL'...), continent réel (piège vérifié : UA9X Komi = Russie EUROPÉENNE, UA0 = Asie), zones CQ réelles → détection « NOUVELLE ZONE » restaurée, sets pays/zones peuplés depuis le log. cty.dat committé et RAFRAÎCHI AUTOMATIQUEMENT au démarrage du serveur s'il a >30 j (update_cty_if_stale : validation stricte avant remplacement, écriture atomique, rechargement à chaud, thread de fond — commit 264382d). Bannière serveur dépoussiérée (plus de mention Rallye).
- A2 : GET /log/check?call&band&mode (nouveau/doublon/nouveau_mult via le moteur + shared_log) ; badge #callStatusBadge sous le champ indicatif (debounce 250 ms, checkCallStatus dans radiocontest_logbook.js).
- A3 : stats coach rate_10min (extrapolé ×6) + rate_60min ; statusbar ⚡ sur toutes les pages, objectif au clic (localStorage rc_rate_goal), vert/rouge.
- C1 : coach.run_sp_recommendation (rate × densité de mults spottés depuis _spots_from_caches) → RUN/S&P/CHANGE affiché en tête du panneau coach. mult_spots_count exposé dans /coach/state.
- Restent de TODO_CAPACITES.md : B2 balises NCDXF/IBP, B3 PSK/WSPR, B4 grey-line, B5 Es prédictif, C2 réglages alertes, D2 rotor, D3 CAT, E2 préfixe→département, E4 météo.
- Bugs corrigés le 16/07 (commit f25feb9) : (1) grille du spotteur prise pour celle du DX (F5ZSW placée à Prague) → scoring.extract_dx_locator valide chaque candidat contre les centroïdes pays cty.dat (dxcc.lookup expose lat/lon ; ATTENTION cty.dat : longitude positive = OUEST) du DX ET du spotteur, branché dans build_ranked_spots + clusters._normalize_spot ; en cas de doute : pas de position. (2) Onglet MONDIAL WA7BNM du calendrier : tri par prochaine occurrence + filtre À VENIR appliqué + setFilter re-rend l'onglet actif (currentTab). 54 tests.

**Bloc pré-concours le 16/07/2026 (commit cea655b) :**
- BUG DU 01/08 TUÉ : HF/VHF du refresh dérivés des bandes de la DÉFINITION (l'ancienne liste HF_CONTESTS codée en dur ignorait EUHFC/WAEDC → cluster vide). EUHFC sans toggles = 15 spots HF vérifiés.
- Config serveur persistante : .server_config.json (gitignoré, contient identifiants ON4KST), écrite par /config/save, rechargée au démarrage — coach/statusbar/mobile opérationnels après redémarrage.
- Mult EUHFC exact : évaluateur scoring 'exchange_distinct' (neutre au spot) + coach.exchange_mult_stats (années 2 chiffres distinctes PAR BANDE depuis num_rcvd) → « N mults → score X pts » dans panneau coach + prompt IA.
- QTC WAE : storage.qtc_log (qtc_log.json gitignoré), POST /qtc/add (plafond 10/station), GET /qtc/list, bouton ✉ QTC du logbook (visible si contest WAEDC*), total dans hints coach. load_qtc_from_disk au démarrage serveur.
- Carte pilotée par le moteur : refreshRankedStations (poll 60 s de /data/spots_ranked) remplace parseStations (re-parsing prose LLM, désormais non appelé) ; lat/lon DXSummit traversent build_ranked_spots quand pas de locator fiable → 40/40 spots positionnables. updateMapStations accepte lat/lon directs.

**Purge du 16/07 (commit 141964e) :** supprimés du working tree (restent dans l'historique git) : serveur.py.bak_pre_phase0 (l'ancien monolithe — le test d'or ne peut plus être recréé depuis le tree, utiliser `git show 4f07e5f:concours/serveur.py` au besoin), logbook.html.backup_tronque, files.zip, RAPPORT_RELECTURE_v3.md, RadioContest_F6KQJ_2026_STABLE_v3.0.zip. Code mort retiré : carte parseStations()+drawClusterSpots() (la carte est 100 % pilotée par /data/spots_ranked), mobile apiModal+API_KEY.

**Refonte lisibilité le 16/07 (commit 51cc77d) :** 252 font-size relevées (8→11, 9→12, 10→12, 11→13, 12→14, 13→14 px, plus rien sous 11 px), palette nuit --text:#E9ECF5 / --muted:#A9B0C8 / --border:#2B2F4A (fini le jaune #FFE566 et l'olive #B8A040), mode jour --muted:#4A5170. SVG boussole/graphe et taille dynamique carte relevés. NB : les captures d'écran Browser pane timeout toujours sur ces pages — vérifier par styles calculés (JS). Retouche bdfb25d : statusbar 14 px/34 px, bandeau réseau 14 px, adresse de partage réelle en lien cliquable + bouton COPIER (initShareLink immédiate — l'ancien fetch /network/info attendait la fin de l'assistant de config, d'où le « TON-IP » figé).

**CAT rigctld (D3) fait le 16/07 (commit 369bd44) :** radiocontest_rig.py (client protocole texte Hamlib : get_state/set_freq/send_morse/stop_morse, verrou séquentiel, timeout 2 s, erreurs → dict jamais d'exception). Endpoints GET /rig/state, POST /rig/qsy|/rig/cw|/rig/stop (400 si désactivé). Désactivé par défaut, activé dans CONFIG section RADIO (rig_enabled/rig_host/rig_port, mode expert) ou config.json section "rig". PROPAG : bouton ▶ QSY par spot. Logbook : widget RADIO (poll 3 s, freq/mode), bande de saisie suit la radio (syncBandModeFromRig), macros F1-F8 partent en CW par le keyer si radio en CW, bouton STOP CW. Testé bout en bout avec un faux rigctld (tests/test_rig.py, 5 tests). rigctld tourne sur le PC relié à la radio (peut être un autre poste LAN) : `rigctld -m <modèle> -r <port> -T 0.0.0.0`.

**Rotor rotctld (D2) fait le 16/07 (commit 43b5ed3), même modèle que le CAT :** radiocontest_rotor.py (get_position/set_position az-el bornés/stop, port 4533, timeout 3 s, erreurs→dict). Endpoints GET /rotor/state, POST /rotor/point|/rotor/stop. CONFIG section ROTOR (rotor_enabled/host/port, expert). PROPAG : bouton 🧭<cap>° par spot. Logbook : bouton 🧭 POINTER sous la boussole de saisie (l'azimut est déjà calculé par bearing). tests/test_rotor.py (6 tests, faux rotctld). Lancement : `rotctld -m <modèle> -r <port> -T 0.0.0.0`.

**Lot propagation & capteurs le 16/07 (commits 3664427→657fe3c) — TOUT le reste du TODO fait :**
- B2 balises NCDXF/IBP : radiocontest_beacons.py (18 balises, rotation (slot-b)%18, cycle 180s) + GET /beacons/now + widget PROPAG (poll 5 s).
- B3 PSK Reporter : radiocontest_psk.py (query API par indicatif, format receptionReport, cache 3 min) + GET /data/heard_where + panneau PROPAG « où mon signal est entendu ». NB : PSK Reporter = modes numériques ; pour la CW c'est le RBN (non implémenté, telnet bloqué). Capteur propag, jamais dans le log/scoring.
- B4 grey-line : solarDeclination + drawGreyLine (terminateur, polygone nuit) dans radiocontest_carte.html, poll 5 min.
- B5 Es/aurora prédictif : coach.es_aurora_forecast (saison mai-août + pics horaires + indice K), champ vhf_forecast, affiché panneau coach.
- C2 alertes : son on/off + volume en CONFIG ; carte alertBeep/flashAlert (WebAudio + bulle + flash onglet) sur nouveau mult spotté (anti-répétition 20 min) et mention ON4KST (on4kst_mentions exposé dans /data/refresh).
- E2 départements : radiocontest_departments.py (102 depts, dept_from_exchange lève l'ambiguïté RST/dept en retirant le RST de tête, department_mult_count) ; coach « Départements reçus : N » pour concours dept_dxcc. Le dept métropolitain vient de l'ÉCHANGE, pas de l'indicatif.
- E4 météo : radiocontest_weather.py (open-meteo, sans clé, cache 10 min, alerte matériel rafales/orage/gel) + GET /data/weather + widget bandeau logbook.
- Revue adversariale du lot (workflow multi-agent, commit fac7563) : 15 findings → 8 faux positifs éliminés, 7 confirmés corrigés. Le plus grave (HIGH) : dept_from_exchange rejetait ~45 départements « RST-like » (13/33/44/59...) car en PRODUCTION num_rcvd = département SEUL (pas « RST dept ») → coach REF sous-estimait le mult de moitié. Autres : garde-fou grey-line équinoxe (split horizontal faux) retiré, alerte orage météo cumulée (plus masquée par rafales), valeurs JSON null protégées, codes WMO complétés, aurora gate is_vhf, amorçage silencieux des alertes mult au 1er chargement.
- 89 tests au total. Le TODO_CAPACITES.md (Downloads) est intégralement traité.**

**Archivage permanent des logs par concours (commit 472d00c) :** radiocontest_archive.py = archive_log() crée archives/<concours>_<AAAAMMJJ-HHMM>/ avec log.json + Cabrillo + ADIF + resume.txt ; list_archives(). POST /log/archive (clear optionnel), GET /log/archives. /log/reset archive un dossier par concours AVANT d'effacer. Bouton logbook « 📦 ARCHIVER ». Dossier archives/ dans le dossier de données utilisateur (mode app) ou concours/ (dev), gitignoré. Survit au changement de concours (vérifié). 109 tests.

**Packaging application autonome (commit 0502e76) :** PyInstaller mono-fichier. radiocontest_bootstrap.py = dossier données inscriptible (%APPDATA%\RadioContestAI Windows, ~/Library/Application Support/RadioContestAI macOS), seed des fichiers référence embarqués (cty.dat/contest_schema/geojson/custom_contests), chdir, ouverture navigateur — seulement si is_frozen() (dev inchangé). radiocontest.spec (hiddenimports = tous les radiocontest_* car imports paresseux ; datas = html/js/json/dat/geojson ; console=True). build_windows.bat + build_macos.sh + INSTALL.md. .exe Windows CONSTRUIT et vérifié (35 Mo, boot+seed OK), dans dist/ (gitignoré). macOS À CONSTRUIRE SUR UN MAC (pas de cross-compilation) via build_macos.sh ou GitHub Actions macos-latest. Port 8080, une seule instance.

**Intégration QRZ.com (commit b7a903d) :** radiocontest_qrz.py = API XML QRZ (auth clé session cachée + ré-auth timeout, lookup nom/QTH/grid/pays/dxcc, cache 24 h, enrichit calldb.json). GET /qrz/lookup?call= (identifiants lus dans config, jamais dans la requête ni renvoyés au client). CONFIG section QRZ (qrz_user/qrz_password). Logbook : fiche QRZ sous le badge indicatif (debounce 600 ms) + pré-remplit le locator si vide. Détails réservés aux abonnés XML QRZ. 105 tests. Sécurité : mot de passe QRZ stocké serveur uniquement (comme on4kst_password).

**Pont WSJT-X FT8/FT4 (commit 19cbaed) :** radiocontest_wsjtx.py = lecteur UDP protocole Qt QDataStream (port 2237). Parse Status (type 1) et QSO Logged (type 5) → chaque QSO numérique validé dans WSJT-X est AUTO-LOGGÉ dans shared_log (source=wsjtx). Écouteur thread de fond, désactivé par défaut, idempotent (boot config.json ou à chaud via /wsjtx/state). add_qso_to_log() factorisé (dédup+scoring) partagé /log/add + pont. CONFIG section WSJT-X (expert), widget logbook ●/○ + fréquence/mode + compteur. Testé bout en bout (vrai datagramme UDP → QSO dans le log). Côté WSJT-X : Réglages→Rapports→UDP Server = IP du PC, port 2237. 98 tests.

**Zéro scroll de page (commit 83f441b) :** les 7 pages vérifiées à 0/0 (H et V). calendrier/configuration/propagation passées en layout « app » : body flex overflow:hidden, .container = zone de défilement interne (flex:1 overflow-y:auto min-height:0) — nav/statusbar/étapes toujours visibles. Modèle à REPRODUIRE pour toute nouvelle page.

**Tableau de chasse départements REF (commits 5dfa027 + 45ecc47 retours utilisateur) :** page refaite PLEIN ÉCRAN (barre progression compacte, carte flex:1 pleine hauteur, grille 8 col. panneau 330 px, zéro scroll), carte STATIQUE France seule (plus de tuiles monde, zoom/drag désactivés, fitBounds, 100 % offline). Détection à 3 niveaux dept_for_qso : échange > calldb(dept) > GÉOGRAPHIE du locator (point-dans-polygone ray casting sur le GeoJSON, réservé indicatifs FR via cty.dat France/Corse) — indispensable car Bol d'Or & co échangent un n° de série, pas le département (cas réel : F1OMQ JN25GO → 69). nouvelle page radiocontest_departements.html (6e onglet nav 🇫🇷 DÉPARTEMENTS) pour les concours REF où l'échange = n° de dept. Carte Leaflet géographique de France (GeoJSON gregoiredavid/france-geojson, 96 dépts, caché dans france_departements.geojson gitignoré, offline après 1er DL via /data/france_geojson) : chaque département passe au VERT une fois contacté. + grille tableau de chasse 01-95/2A/2B + outre-mer + barre « X/96 · encore N pour verdir la France ». Alimenté par /data/departments_worked (dep.departments_progress depuis les échanges du log). Refresh 20 s. Nav désormais à 6 onglets sur toutes les pages.

**Menu multilingue (commit 0c011df) :** radiocontest_i18n.js = moteur i18n CÔTÉ CLIENT par correspondance de texte source français (AUCUNE balise data-i18n à poser). TreeWalker sur les nœuds texte + attributs title/placeholder ; sélecteur `<select id="rcLangSelect">` injecté dans le header de chaque page ; inclus juste après radiocontest_statusbar.js sur les 7 pages. 8 langues traduites à la main dans le dictionnaire `T` (fr/en/de/es/it/pt/nl/pl) + option **auto** (restaure le français d'origine pour laisser le navigateur traduire vers n'importe quelle autre langue — réponse utilisateur « 3 et 4 »). Choix persistant (localStorage `rc_lang`, défaut 'fr') et synchronisé entre onglets (storage event). window.rcTranslate() pour re-traduire le contenu injecté dynamiquement. DEUX pièges corrigés et vérifiés : (1) préfixe emoji des liens de nav ignoré à la correspondance (regex `^([^\p{L}]+?\s*)(\p{L}.*)$/u` → « CARTE IA » traduit dans « 🗺️ CARTE IA ») ; (2) traduction TOUJOURS depuis le français d'origine mémorisé (WeakMap `ORIG`), sinon un changement en→de échouerait (on chercherait « AI MAP » dans le dico allemand). Extensible : ajouter une langue = 1 entrée `LANGS` + 1 bloc dans `T`. Vérifié navigateur : nav+libellés traduits dans les 8 langues, persistance inter-pages, retour FR/auto OK, zéro erreur console.

**Agent IA + coach multilingues (commit 826dd2a)** — le menu ne changeait que le châssis statique ; l'agent LLM répondait toujours en français et le panneau coach restait français (contenus injectés en JS + générés serveur, invisibles au moteur de correspondance). Trois leviers : (1) **Agent LLM** : `window.rcLangDirective()` (map LLM_DIRECTIVE 7 langues dans radiocontest_i18n.js) ajoute « réponds dans cette langue » au prompt système CÔTÉ CLIENT avant `/proxy/ai` (radiocontest_carte.html send + autoCheckSilently, radiocontest_mobile.html) — vide en fr/auto. (2) **Contenu injecté** (messages agent, boutons, placeholder, horloge/rythme coach) : dico étendu `T_AGENT` (fusionné dans T) + helpers `window.rcT`/`rcTf`/`rcGetLang` + **MutationObserver** dans init() qui retraduit le DOM ajouté après coup — débounce **setTimeout (PAS requestAnimationFrame, gelé quand l'onglet est masqué** — cas courant en concours, WSJT-X devant ; piège vécu et corrigé). La carte utilise des shims locaux `rcT/rcTf/rcLangDirective/rcLang` (repli identité). (3) **Coach déterministe** (hints/reasons/vhf_forecast/band reasons, textes interpolés donc intraduisibles par correspondance) : localisation SERVEUR via `radiocontest_coach_i18n.py` (nouveau) = table `t(lang, key, **params)`, 34 clés × 7 langues, placeholders `{h:.0f}`/`{r10}`… préservés, FR = source de vérité (fallback). `lang` propagé à `build_coach_state`→band_plan/build_hints/es_aurora_forecast/run_sp_recommendation ; le client passe `?lang=` (query parsé dans radiocontest_http.py `/coach/state`). `coach_prompt` reste FR (le LLM répond dans la langue via la directive). Traductions produites par workflow (1 agent traducteur/langue) ; PIÈGE : l'agent EN avait HTML-échappé `&` (`Search &amp; Pounce`) → décodage `html.unescape` à l'assemblage. 109 tests OK (coach FR byte-identique). Vérifié navigateur en/de/es/it/pt/nl/pl.

**Paquet REF (commit c2b041f, livré la veille du Bol d'Or QRP 18/07 14h00 UTC)** : (1) **Super Check Partial** — radiocontest_callhistory.py (nouveau) = index call→{dept, locator, qso_count, last_date} fusionnant calldb.json (18 852 indicatifs, 13 564 avec dept, 6 665 avec locator) + table SQLite qso_archive (85 anciens QSO, jamais lue avant) + archives/*/log.json + shared_log ; TTL 300 s, update_from_qso à chaud depuis add_qso_to_log ; GET /call/index remplace /calldb.json dans loadCallDB (repli conservé) ; searchCalls = fragment dès 3 car. (« 6KQ »→F6KQJ), déjà-travaillés en tête (tag ✓ DÉJÀ VU, src 'hist'). (2) **Pré-remplissage échange** : exchange_wants(cdef) ('dept' si dept_dxcc, 'locator' si km_x_locators) ; applyCallData pré-remplit #inputNumRcvd (dept) si currentExchange.label_r contient 'DEPT' — le locator était déjà pré-rempli, désormais nourri par tout l'historique. (3) **Validateur** — radiocontest_validator.py (nouveau) : doublon call+bande (règle REF), locator manquant/invalide (=0 km), distance suspecte par bande (MAX_KM_BY_BAND, 144→1400 km), dept invalide pour stations France/Corsica (dxcc.lookup country), préfixe inconnu cty.dat, hors fenêtre (réutilise coach._parse_dt), RST vide ; niveaux erreur/attention/info, MAX 200 ; GET /log/validate + bouton 🔍 VÉRIFIER logbook (overlay #validateOverlay cloné du pattern checklist, Échap géré). (4) **Débrief IA** — coach.build_debrief : QSO/heure, best/worst hours, km+ODX par bande (locator_to_latlon+haversine, pad 'MM' si 4 car.), silences ≥30 min, depts si dept_dxcc → debrief_prompt ; GET /coach/debrief + bouton 🎓 DÉBRIEF carte (garde « rien à débriefer » si 0 QSO). (5) **Chasse aux départements** — departments.department_targets : manquants × stations connues (index inversé dept→calls) × spots actuels (_spots_from_caches, spotté avec fréquence = cible immédiate, en tête) ; GET /departments/targets + panneau 🎯 CIBLES page départements (poll 60 s, seulement l'actionnable). Nouveaux libellés traduits 7 langues (T_AGENT). 120 tests (11 nouveaux dans tests/test_ref_features.py). RAPPEL pièges : /calldb/lookup HamQTH écrase l'entrée calldb et PERD dept (http.py:559-562, à corriger un jour) ; REF THF/QRP = serial+locator (dept via calldb/géo seulement), REF HF = dept dans num_rcvd.

**Carnet permanent — inspiré Log4OM/HRD (commit 6ec1dc8)** : le logiciel était 100 % orienté concours ; ajout de 3 capacités de logger généraliste raisonnant sur TOUTE la vie de la station. (1) **Panneau « déjà contacté »** — `radiocontest_awards.py` (nouveau) : `collect_all_qsos(shared_log)` fusionne log actif + archives/*/log.json + table SQLite qso_archive (dédup par call+band+mode+date+time, cache TTL 120 s, invalidate()), enrichit chaque QSO (dxcc_country/continent/cq_zone via dxcc.lookup, dept via departments.dept_for_qso). `history(call)` (tous QSO passés + statut confirmé), `new_one(call, band)` (nouveau PAYS/DÉPARTEMENT à vie, pas seulement dans le concours). GET /call/history?call=&band= → history + new_one. Client : `checkPrevQsos` (debounce 350 ms) branché dans onCallInput après lookupQRZ, panneau #prevQsos sous les badges, nettoyé dans clearForm. Hook add_qso_to_log → callhistory.update_from_qso (déjà là). (2) **Diplômes + QSL** — `award_summary` (DXCC pays travaillés/confirmés, départements metro/DOM, continents, par bande) → GET /awards/summary ; bouton logbook 🏅 DIPLÔMES (overlay #awardsOverlay) + boutons upload eQSL/ClubLog & sync LoTW. `radiocontest_qsl.py` (nouveau) : upload ADIF multipart vers eQSL/ClubLog (réutilise export.build_adif), téléchargement rapport LoTW (login/password, PAS de TQSL pour descendre) + **parseur ADIF** (`_parse_adif_records` — PIÈGE CORRIGÉ : `<EOR>`/`<EOH>` n'ont pas de `:longueur`, ma 1re regex les ratait → tous les records fusionnaient ; regex à groupe longueur optionnel + branche balise-de-contrôle) ; bande ADIF '2m'→'144' (inverse export.ADIF_BAND) pour aligner la clé `CALL|MHz|MODE` sur le log. Confirmations dans `qsl_confirmations.json` (partagé awards↔qsl), horodatage `qsl_sync.json`. POST /qsl/upload (body {service}), /qsl/sync (LoTW), GET /qsl/status. Section CONFIG « QSL & DIPLÔMES » (eqsl_user/password, lotw_user/password, clublog_email/callsign/password/api_key) placée après QRZ dans panel4, save (saveConfig ~2113) + load (loadSavedConfig ~2256, la 2e def est l'active — pièges de doublons de fonctions). Identifiants côté serveur (config puis config.json section 'qsl'). (3) **Band map graphique** — colonne 196px du logbook (#bandmapPanel, CSS inline, cachée <1100px) entre .saisie-panel et .log-panel : `refreshBandMap` fetch /data/spots_ranked, filtre currentBand, trie par fréquence décroissante, couleur par priorité/new_mult (★ mult, barré si already_done), marqueur ▶ TX inséré à rigState.freq_khz, clic = remplit #inputCall + POST /rig/qsy ; rafraîchi dans setBand + 15 s. Libellés DIPLÔMES/DÉJÀ CONTACTÉ traduits 7 langues (T_AGENT). 130 tests (10 nouveaux tests/test_awards_qsl.py, dont le parseur ADIF). PIÈGE VÉRIF : le javascript_tool sur un état de page à moitié navigué renvoie « toutes fonctions undefined » — recharger proprement d'abord (le fichier parse OK via new Function).

**Correctifs UX (commit 5919af9)** : (1) le MutationObserver i18n se redéclenchait CHAQUE seconde car l'horloge/compte-à-rebours réécrit du texte via textContent (= mutation childList d'un nœud texte) → en langue ≠ fr il retraduisait tout le corps chaque seconde → le libellé « TEMPS RESTANT » clignotait. FIX : l'observer ne réagit plus qu'à l'ajout d'un nœud **ELEMENT** (nodeType===1), pas aux remplacements de nœuds texte seuls (règle générale : les compteurs/horloges mis à jour par textContent ne doivent PAS déclencher rcTranslate). + setCountdownLabel n'écrit le libellé que sur changement de phase et retraduit une seule fois. (2) band map filtré par **plage de fréquence** de la bande courante (_BM_RANGE, 2 m = 144–148 MHz) au lieu de l'étiquette `band` → un spot 50/432 mal étiqueté ne peut plus apparaître sur 2 m (un band map = une seule bande).

**8 fonctions « à la HRD/Log4OM+ » (commits 8295e65 Lot 2+backend, f6e8e08 Lot 1+config Lot 3)** — tous les moteurs déterministes sont testés (tests/test_propagation_plus.py, 141 tests au total). LOT 1 (opérer, radiocontest_logbook.js/.html) : (a) **keyer vocal** phonie = MediaRecorder 4 slots WAV base64 (localStorage rc_voice), panneau #voicePanel en SSB/FM, macros CW en CW (updateKeyerPanels selon rigState.mode) ; (b) **ESM** (Enter Sends Message) N1MM = bouton #esmBtn, esmHandleEnter enchaîne CQ→échange→log+merci via keyer CW (copyMacro idx 0/1/2) ou vocal (slots V1/V3/V4), hook esmSend('tu') à la validation ; (c) **décodeur CW** Web Audio = getUserMedia→AnalyserNode→Goertzel 450-950 Hz→chrono points/traits (dot adaptatif)→Morse→texte (#cwDecodePanel, table MORSE). LOT 2 propagation (nouveaux modules + page propagation panneaux) : **tropo** (radiocontest_tropo.py, gradient réfractivité N depuis open-meteo niveaux pression, ducting/super/normal/sous, GET /data/tropo), **météores** (radiocontest_meteors.py, calendrier MS 10 essaims, GET /data/meteors), **avion** (radiocontest_aircraft.py, ADS-B adsb.lol + géométrie réflexion axe-DX, GET /data/aircraft?bearing=, fetch ~250 km pour la vitesse), **RBN CW** (radiocontest_rbn.py, telnet reversebeacon.net:7000 — MARCHE sur ce réseau —, où mon signal CW est entendu, GET /data/rbn). LOT 3 compétition/sécurité : **scoreboard** (radiocontest_scoreboard.py, snapshot score + XML N1MM → contestonlinescore, POST /scoreboard/push + thread), **sauvegarde** (radiocontest_backup.py, copie .db+.json+.adi horodatée vers backup_folder configurable type SynologyDrive, rétention 20, POST /backup/now + thread + bouton 💾 SAUVEGARDER). Threads de fond dans radiocontest_serveur.py (_scoreboard_loop/_backup_loop, lisent current_config sous config_lock). Config panel4 section « COMPÉTITION & SÉCURITÉ » (scoreboard_enabled/interval, backup_folder/interval, rbn_enabled). Fichiers d'état runtime gitignorés. PIÈGE ADS-B : adsb.lol lent (~20 s à 400 km) → rayon réduit à 250 km + cache 20 s. POST HTTP sortant : réutiliser le pattern radiocontest_qsl._post (SSL_CTX).

**Retraits/ajustements post-livraison :** (a) **diffusion par avion SUPPRIMÉE** (commit 14e9e44, jugée trop lourde/ADS-B lent) : radiocontest_aircraft.py effacé, endpoint /data/aircraft, panneau+loadAircraft, toggle prop_aircraft config, tests avion — tout retiré. (b) **Page propagation refaite en maçonnerie de cartes de LARGEUR UNIFORME** (CSS `column-width:390px` fixe, plus de grille avec cartes span-2 hétérogènes) : toutes les cartes même largeur (403px vérifié), listes longues plafonnées `.scroll-list` (max-height 340, scroll interne) → zéro scroll de page, lecture homogène. (c) **Config « MA STATION » réordonnée** (commit 8be50d1) : essentiel d'abord (Identité→Opérateurs→Station→Antennes→Matériel/EDI→IA), matériel piloté (WSJT/CAT/ROTOR) tout en bas ; section DATES déplacée de MA STATION vers l'étape CONCOURS (sous la sélection, pré-remplie par le concours choisi). (d) zone de saisie logbook réduite de 10 % (48 %). Reste 7 des 8 fonctions (keyer vocal, ESM, décodeur CW, tropo, météores, scoreboard, RBN, sauvegarde) + carnet permanent + SCP + validateur + débrief.

**Ouvertures par région + analyse IA persistante (commit 8c6725a) :** (1) **radiocontest_paths.py** = depuis N'IMPORTE QUEL QTH (ex. Hiva Oa), estime par bande la proba d'ouverture vers 7 régions DX (EU/NA/SA/AF/AS/JA/OC). Heuristique : distance + azimut court/long chemin, jour/nuit aux 2 extrémités (sun_elevation), grey-line, MUF+SFI+K. GET /data/openings[?region=EU]. **context_block injecté dans le contexte agent via do_refresh** (bloc « PROPAGATION PAR RÉGION ») → l'IA répond « mes chances vers l'Europe ? ». Bouton 🌍 OUVERTURES sur la carte. (2) **Analyse IA côté serveur** (survit au changement d'onglet — les liens nav rechargent la page et tuaient le fetch) : `call_llm(cfg, system, messages)` factorisé des 3 fournisseurs (module-level dans http.py) ; POST /agent/analyze lance un THREAD (stock `_agent_analyses` id→{status,reply,error}, rétention 10, `_agent_lock`) ; GET /agent/analyze/state?id=. Carte : send() poste /agent/analyze, mémorise l'id (localStorage rc_analyze), `pollAnalyze` (1,5 s) ; `resumeAnalyze()` à l'init reprend l'analyse au retour. Vérifié navigateur (résultat repris après départements→carte). PIÈGE relevé (pré-existant, non corrigé) : la carte reposte sa config localStorage à /config/save au chargement (applyConfig ~660) → peut écraser api_key de current_config si le client ne l'inclut pas (la clé vit en localStorage radiocontest_apikey_<provider>) ; touche aussi l'ancien /proxy/ai. 142 tests.

**Cluster pleine hauteur + MODE EXPÉDITION (commit c747e4e) :** (1) page propagation : cluster NEED LIST déplacé en colonne DROITE pleine hauteur (layout flex : `.prop-main` maçonnerie à gauche, `.prop-cluster` 440px/100% hauteur à droite, spotList `flex:1;overflow-y:auto`) — lecture complète des spots (449px visibles vs 340). Responsive : empile <1000px. (2) **radiocontest_wall.html** = écran mural grand écran/projecteur : flux QSO temps réel gros caractères, total/rythme/stations/ODX, barres par bande + chips mode/opérateur, flash nouveaux QSO ; GET /data/wall (radiocontest_wall.py, wall_state(shared_log,cfg) déterministe), poll 3 s ; ouvrable via lien config ou URL directe. (3) **Club Log Live Stream** : qsl.realtime_push(cfg,qso) POST clublog.org/realtime.php (identifiants ClubLog de la section QSL) ; hook fire-and-forget dans add_qso_to_log si `clublog_live` activé. (4) Config section « 🏝️ MODE EXPÉDITION » (toggle clublog_live + lien mur). Log commun = shared_log multi-op existant (chaque poste = un navigateur sur le WiFi, sélectionne son OP). 144 tests. RAPPEL : le dédup log est call+band+mode+contest → en expédition les QSO cross-band/mode ne sont PAS des doublons (voulu).

**Carte de propagation mondiale 24 h (commit 22ff3f5)** : surcouche colorée sur la carte IA. `paths.prop_grid(my, band, when, solar)` = grille mondiale (264 cellules, pas 15°) de scores d'ouverture depuis le QTH ; `_dist_factor` donne des empreintes réalistes (basses=proche/médium, hautes=DX) ; band='best' = meilleure bande/point ; cache par (bande, heure, solaire). GET /data/propmap?band=&hour=0..24. Carte IA (radiocontest_carte.html) : panneau flottant « 🌍 PROPAGATION » (#propControl, case on/off) + sélecteur de bande + **curseur 24 h** + bouton PLAY (animation) + légende dégradée ; togglePropMap/loadPropMap/drawPropMap = L.rectangle colorés (propColor gris→rouge→jaune→vert, semi-transparents, bringToBack sous les spots), propLayer. NB : les bandes sont en MHz ('10'=10 MHz/30m, pas 10 mètres).

**Scoring logbook unifié le 18/07 (matin du Bol d'Or QRP)** : calcPoints (radiocontest_logbook.js) lit désormais les barèmes serveur — loadScoringDefs() charge les blocs scoring de /data/calendar dans contestScoringDefs, evalPointsFromDef évalue les briques côté client (BRICK_PREDICATES via lookupDXCC, filtres bands/modes/prefix_in, validity nom ou {prefix_in}, same_square_points, per_km/params ; LEGACY_JS_BRICKS pour les types historiques). L'ancienne table JS codée en dur (8 concours, tout le reste retombait sur 1pt/km — le WAE aurait affiché des scores délirants) ne sert plus que de repli hors-ligne. Vérifié en réel sur le serveur du jour : 36 barèmes chargés, WAE=1pt fixe + EU rejeté, ARRL 10m CW=4/SSB=2 (nouveau filtre 'modes' sur les règles de points : moteur+schema+validateur), EU HF is_eu, REF_QRP inchangé (73 km = 73 pts). Complété : schema/validateur pour 'modes', test test_dxcc.pas_de_maj_si_fichier_recent défloké (os.utime — il cassait dès J+1 après le téléchargement de cty.dat). 154 tests OK. NB : les points d'un QSO sont calculés CÔTÉ CLIENT à la saisie (add_qso_to_log ne score pas) — le pont WSJT-X loge donc sans points logbook (à unifier un jour côté serveur).

**Prochaine étape : Phase 5 terrain** — Bol d'Or des QRP 18-19/07 (SCP + validateur + débrief + déjà-contacté + band map prêts ; ARCHIVER les QSO d'essai avant 14h00 UTC), EU HF Championship 01/08 (définition prête, spots HF OK, mults exacts), WAE CW 08-09/08 (définition + QTC prêts, scoring logbook désormais correct).

**Pilotage radio CAT natif (commit 6285f77, 18/07/2026)** — demande initiale : reprendre `C:\Users\parri\SynologyDrive\RADIOAMATEUR\commande radio pc\Commandes_Pilotage_Radio_PC.md` (HORS de ce dépôt git, ne pas chercher à le committer avec le projet) et construire un vrai pilotage radio "via le programme" plutôt que de dépendre de rigctld externe.
- Recherche manufacturer approfondie (3 agents parallèles, sources officielles Icom/Yaesu/Kenwood/Elecraft) → doc de référence largement complété (PTT/S-mètre/puissance/split/transceive-mode/tables ID par marque) ET un bug trouvé : l'exemple BCD Icom du doc original pour 145.500.000 Hz était FAUX (`00 50 45 01 00` au lieu de `00 00 50 45 01`, vérifié par calcul + round-trip contre un exemple de référence connu 145.000.000→`00 00 00 45 01`) — corrigé avant d'en dépendre pour le code.
- `radiocontest_cat.py` (nouveau) : pilote CI-V binaire (Icom/Xiegu) + pilote ASCII générique (Yaesu/Kenwood/Elecraft, tables de mode et split par marque). Logique de trame 100% pure (`civ_*`/`ascii_*`, bytes/str in-out) testée sans matériel ; `SerialPort` (pyserial) est la SEULE classe qui touche le port réel. `RigManager` pour multi-radio (base SO2R future). `autodetect()` : ASCII `ID;` en premier (fiable Yaesu/Kenwood, ambigu Elecraft K3/K3S/KX3/KX2 tous à 017), repli CI-V `19 00` (jamais certain, adresse réassignable). Couche haut niveau pilotée par cfg (`cat_settings`/`get_state`/`set_freq`/`test_connection`) avec connexion PERSISTANTE réutilisée (clé port+marque+modèle+baud, reconnecte si un de ces champs change) — testable via injection `radiocontest_cat._open_serial` (factory remplaçable, jamais de vrai port en test).
- Piège CI-V corrigé pendant les tests : `civ_parse_frame` avait les deux octets d'adresse mal étiquetés (le protocole est symétrique — même position = destinataire dans une requête PC→radio, expéditeur dans une réponse radio→PC ; mal nommer "addr_from/addr_to" fait échouer silencieusement TOUTES les commandes CI-V). Renommé en positions neutres `addr_dest`/`addr_src`.
- Piège parsing `IF;` ASCII : positions de champ calculées depuis le DÉBUT de la trame (mem/freq/offset/RIT/XIT/mode), pas depuis la fin — la longueur totale varie en QUEUE (scan/CTCSS) selon le modèle, pas en tête. Vérifié en construisant des trames champ par champ avant d'écrire le test (jamais fait confiance à un comptage de position à la main après le bug BCD).
- `radiocontest_rig.py` (rigctld) NON remplacé, complété : `/rig/state` et `/rig/qsy` dispatchent selon `cat_mode` (natif vs rigctld) sans rien casser pour les configs existantes — migration vérifiée en navigateur (`rig_enabled:true` sans `cat_mode` → bascule auto en `cat_mode:'rigctld'`, jamais de bascule silencieuse vers le natif qui n'a ni port ni marque configurés). Nouveaux endpoints `GET /rig/ports`, `POST /rig/connect_test` (éphémère, ne touche jamais la connexion persistante du polling).
- RÉGRESSION trouvée et corrigée pendant la vérif navigateur : en remplaçant l'ancien sélecteur simple `rig_enabled` par `cat_enabled` unifié, j'avais supprimé l'élément DOM `rig_enabled` mais laissé une ligne `document.getElementById('rig_enabled').value=...` non protégée dans `loadSavedConfig()` → plantait (silencieusement, try/catch) sur TOUTE config existante ayant `rig_enabled:true`, arrêtant la restauration des champs suivants. Leçon : avant de conclure "ça marche", toujours tester le chargement d'une config LEGACY (sans les nouveaux champs), pas seulement le round-trip d'une config neuve.
- `radiocontest.spec` : hiddenimports += `cat` + sous-modules `serial.tools.list_ports_*` (détection par OS).
- 41 nouveaux tests (`tests/test_cat.py`), 204 au total. Vérifié en navigateur réel : rendu de la section RADIO, peuplement marque→modèle, `/rig/ports` répond `[]` proprement (pas de port physique sur la machine de dev), `/rig/connect_test` renvoie une erreur propre sur port inexistant (502, message clair), round-trip config natif complet, migration config legacy sans crash. Jamais testé contre une VRAIE radio physique (aucune connectée) — les positions de champ `IF;` (mode) sont dérivées de la description textuelle des manuels, pas d'une capture réelle : à confirmer sur matériel au premier usage réel.

**Spots filtrés par concours + analyse IA persistante — TERMINÉ ET COMMITTÉ (commit da52d88, 18/07/2026)** — suite à un retour terrain réel du Bol d'Or QRP ("l'IA propose des contacts farfelus trop loin, bandes non autorisées type 50 MHz alors que le concours ne les accepte pas, spots FT8 en concours NO-DIGI", "si je change d'onglet l'analyse est interrompue").
- `radiocontest_scoring.py` : `filter_spots_for_contest()` écarte les spots hors bandes/modes du concours actif, hors fenêtres FT8/FT4 si NO-DIGI, distance non plausible par bande (`_MAX_PLAUSIBLE_KM`), incohérence fréquence/bande (`_band_from_freq` — un spot 50.125 MHz étiqueté "144" polluait 144 ET 432, déchet de parsing). Branché dans `build_ranked_spots`. Contexte IA enrichi d'une consigne explicite + état vide honnête si aucun spot exploitable (plus d'invention).
- `radiocontest_clusters.py` : suppression du fallback DXMaps qui appariait n'importe quel jeton en forme d'indicatif avec n'importe quel locator de la page (fabriquait des spots fantômes, ex. `FF00FF`).
- `/agent/analyze` (radiocontest_http.py) + `radiocontest_carte.html` : l'enrichissement contexte (do_refresh, ~25s) se fait maintenant DANS le thread serveur plutôt qu'avant l'envoi côté client.
- **Vérifié bout en bout en navigateur avec un VRAI appel IA** (pas juste des tests unitaires) : analyse lancée sur une page, navigation vers 2-3 pages différentes, état "done" récupéré depuis un contexte totalement déconnecté — testé en mode simple ET en mode enrichi par le contexte concours (réponse a bien dit "aucun spot exploitable" + proposé une action concrète au lieu d'inventer, contest étant vide au moment du test).
- PIÈGE COÛTEUX rencontré pendant cette vérification, à ne JAMAIS refaire : `radiocontest_carte.html` reposte sa config localStorage vers `/config/save` à chaque chargement de page (bug pré-existant connu, non corrigé — voir plus haut "PIÈGE relevé"). Naviguer vers cette page depuis le navigateur de test AVEC UN LOCALSTORAGE INCOMPLET écrase `api_key` côté serveur (`.server_config.json`) — ça m'est arrivé 3 fois de suite pendant les tests radio ET cette vérification, effaçant la vraie clé Anthropic de l'utilisateur, restaurée à chaque fois depuis `concours/clef API.txt` (le fichier existe justement pour ça). **Pour toute vérification navigateur future : ne JAMAIS naviguer vers `radiocontest_carte.html` ou `radiocontest_configuration.html` avec un profil de navigateur de test dont le localStorage ne contient pas la vraie config — tester depuis d'autres pages (calendrier/départements/propagation ne repostent pas) et n'appeler `/config/save` qu'avec un payload complet reconstruit, jamais via `saveConfig()` du DOM sur une page fraîchement chargée.** Champs collatéraux perdus et non récupérables (contest sélectionné, ville) : signalés à l'utilisateur, à re-saisir (30 secondes).

**Pilotage radio TCI pour transceivers SDR (commit cdad378, 18/07/2026)** — demande initiale : l'utilisateur a fourni un zip d'une appli tierce (TCI-Remote v3.0, HTML/JS 19k lignes) + 2 URL (cat4om.com, pure-editions.com/.../TCI_Protocol_Reference.html) et demandé un mode TCI pour SDR, en plus du CAT natif et rigctld existants.
- Recherche protocole en 2 temps : (1) grep de l'appli tierce pour en extraire uniquement les COMMANDES DE FIL (`vfo:`, `modulation:`, `trx:`, `cw_msg:`...) sans jamais lire/copier son code UI (discipline copyright, comme pour QSO Director) ; (2) `WebFetch` a échoué à extraire le texte du PDF officiel *TCI Protocol* v2.0 (russe, streams compressés) mais a sauvé les bytes bruts dans un `.bin` — copié en `.pdf` dans le scratchpad puis lu avec `pypdf` (installé via pip, PAS `pdftoppm`/poppler qui manquait) : c'est le contournement à refaire si `Read`/`WebFetch` échouent sur un PDF complexe. Spec officielle obtenue intégralement (41 pages) : port réseau par défaut confirmé par recherche web croisée = **50001** pour ExpertSDR3 (40001 pour l'ancien ESDR2, absent du PDF lui-même).
- **Piège d'environnement découvert** : `pip` et `python3` de ce poste pointent vers DEUX installations Python différentes (`pip`→Python 3.13 Program Files, `python3`→Python 3.14 WindowsApps) — un `pip install X` peut réussir sans que `python3 -c "import X"` le voie. Toujours vérifier avec `python3 -c "import sys; print(sys.executable)"` avant de conclure qu'une lib installée est absente. C'est aussi CE Python 3.13 (pas 3.14) qui fait tourner l'appli réelle (pyserial y est installé) — utiliser cet interpréteur explicite pour tout test/lancement.
- `radiocontest_tci.py` (nouveau) : contrairement à CI-V/ASCII (série, requête/réponse), TCI est réseau (WebSocket RFC 6455) et PUSH (le serveur notifie tout changement de lui-même, pas besoin d'interroger). Aucune lib websocket dispo dans l'environnement (`websockets`/`websocket-client` absents, pas de requirements.txt dans ce projet) → client WebSocket écrit à la main (`WebSocketClient` : handshake HTTP Upgrade, frames masquées en émission/non masquées en réception, pong auto sur ping), dans le même esprit que `radiocontest_rig.py` (TCP brut pour rigctld). `TciClient` maintient un cache d'état alimenté par un fil de lecture continu (`vfo`/`modulation`/`trx`/`rx_channel_sensors`/`callsign_send`) ; `cw_msg`/`cw_macros` pour l'envoi CW (échappement `:`→`^`, `,`→`~`, `;`→`*` par la spec §3.2.1). Couche cfg-driven (`tci_settings`/`get_state`/`set_freq`/`send_cw`/`test_connection`) avec connexion persistante réutilisée, même pattern que `radiocontest_cat.py` (`_open_ws` = point d'injection testable, jamais de vrai socket en test).
- Intégré au dispatch existant `radiocontest_http.py` (`/rig/state`, `/rig/qsy`, `/rig/cw`, `/rig/stop`, `/rig/connect_test`) : `cat_mode` devient un choix à 3 valeurs natif/tci/rigctld (le CW natif reste indisponible, message adapté pour proposer rigctld OU TCI désormais). UI `radiocontest_configuration.html` : option TCI dans le sélecteur de mode + section champs host/port dédiée (`catTciFields`), `updateCatModeVisibility()` étendu à 3 branches, `testCatConnection()` bifurque sur `mode==='tci'`. `radiocontest.spec` : hiddenimports += `tci`.
- **PIÈGE NOUVEAU (à surveiller désormais)** : le classificateur de permission auto-mode a BLOQUÉ la navigation directe vers `radiocontest_configuration.html` pendant cette session (jamais vu avant) — probablement une heuristique de prudence sur une page connue pour manipuler une clé API. Contournement utilisé avec succès : naviguer vers une page sûre (`radiocontest_calendrier.html`), obtenir le cookie de session, puis piloter `/config/save` + les endpoints à tester via `fetch()` en JS depuis cette page (jamais charger `configuration.html` elle-même). Un faux serveur TCI (script Python minimal, WS handshake + echo VFO + confirmation CW) a permis une vérification bout en bout RÉELLE (pas juste des tests unitaires) : `/rig/connect_test`, `/rig/state`, `/rig/qsy`, `/rig/cw`, `/rig/stop` tous vérifiés contre un vrai socket, config restaurée bit-à-bit après coup (diff vide).
- 26 nouveaux tests (`tests/test_tci.py`, dont 4 contre un mini-serveur TCP réel pour valider le handshake/framing WebSocket octet par octet — pas seulement mocké), 240 au total (tous verts). Doc de référence `Commandes_Pilotage_Radio_PC.md` (hors dépôt git) : nouveau §7 TCI inséré avant Hamlib, sections renumérotées 8-11.
- Signalé en tâche séparée (hors périmètre TCI) : `radiocontest_import` (module ADIF du commit précédent) manque des hiddenimports PyInstaller — plante dans l'exécutable packagé, pas en dev.

Lancement : `LANCER_RADIOCONTEST.bat` ou `python serveur.py` depuis `concours/` (port 8080). Le cwd doit être `concours/` (les caches JSON sont relatifs au cwd).
