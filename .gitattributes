# Libs tierces vendorisées (repli CDN local, zone blanche) : stockées VERBATIM,
# byte-identiques à l'amont — aucune conversion de fin de ligne, aucun diff.
concours/vendor/** binary

# Exception : les README de doc dans vendor/ sont du texte à réviser — garder
# des diffs lisibles (sinon la règle binary ci-dessus les rendrait opaques).
concours/vendor/**/README.md text diff
